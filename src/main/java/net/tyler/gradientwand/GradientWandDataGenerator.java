package net.tyler.gradientwand;

//? if fabric {
/*import net.fabricmc.fabric.api.datagen.v1.DataGeneratorEntrypoint;
import net.fabricmc.fabric.api.datagen.v1.FabricDataGenerator;

// Fabric only. NeoForge has no DataGeneratorEntrypoint and this generator is an empty
// stub, so rather than port a no-op the whole class is left out of that node.
public class GradientWandDataGenerator implements DataGeneratorEntrypoint {
	@Override
	public void onInitializeDataGenerator(FabricDataGenerator fabricDataGenerator) {

	}
}
*///?}
